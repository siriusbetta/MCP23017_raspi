cmake_minimum_required(VERSION 3.15)
project (mcp23017)
set(SRC "src/MCP23017.cpp")
add_library(${PROJECT_NAME} ${SRC})
target_link_libraries(${PROJECT_NAME} i2c)
target_include_directories(${PROJECT_NAME} PUBLIC "src")
